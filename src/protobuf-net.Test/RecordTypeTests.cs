namespace ProtoBuf.Test
{
    public class RecordTypeTests
    {
        // no compiler currently works with these

        /*
        public data class LongHand
        {
            public string FirstName { get; init; }
            public string LastName { get; init; }
        }
        public data class FieldsSpecified { string FirstName; string LastName; }
        public data class Positional(string FirstName, string LastName);
        */
    }
}
