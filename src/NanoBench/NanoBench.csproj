<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFrameworks>net10.0;net472</TargetFrameworks>
    <RootNamespace>ProtoBuf.Nano.Bench</RootNamespace>
    <IsPackable>false</IsPackable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- PBN9001: [ProtoModel] is [Experimental]; PBN2010: the runtime-model calls it flags ARE
         the measured legacy baseline here, not migration debt - benchmarks are the one place
         that advice is deliberately declined -->
    <NoWarn>$(NoWarn);CS1591;PBN9001;PBN2010</NoWarn>
    <!-- nano-core micro-benchmarks: see docs/nano-core.md - "functionally correct" is step 0 -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="BenchmarkDotNet" />
    <!-- descriptor.proto composite: Google.Protobuf is the home-turf competitor row, and
         protobuf-net + Reflection self-generate the payload (descriptor.proto's own descriptor) -->
    <!-- aliased: protobuf-net.Reflection's DTOs share the Google.Protobuf.Reflection namespace,
         so the package must be reached via gpb:: (the extern-alias situation AGENTS.md documents) -->
    <PackageReference Include="Google.Protobuf" Aliases="gpb" />
    <!-- the raw reader core lives in Core (internal, IVT'd to this rig) -->
    <ProjectReference Include="..\protobuf-net.Core\protobuf-net.Core.csproj" />
    <ProjectReference Include="..\protobuf-net\protobuf-net.csproj" />
    <ProjectReference Include="..\protobuf-net.Reflection\protobuf-net.Reflection.csproj" />
    <!-- referenced as an analyzer, so the generator runs for real during this project's build:
         the descriptor DTOs carry [ProtoContract], and the raw read pass (symbol-gated on the
         raw surface) emits RawRead_ methods that the equivalence gate compares against
         the hand-written readers -->
    <ProjectReference Include="..\protobuf-net.BuildTools\protobuf-net.BuildTools.csproj"
                      OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
